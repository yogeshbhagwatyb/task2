pipeline {
    agent any



        stage('Build') {
            steps {
                // Use Maven to build your Java project.
                sh 'mvn clean install'
            }
        }

}
